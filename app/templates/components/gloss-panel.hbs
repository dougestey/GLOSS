{{gloss-shard
  label="Current ship"
  value=ship.name
  loaded=loaded
  loadOrder=4
  }}

{{gloss-shard
  label="Status"
  value=(if character.online 'Online' 'Offline')
  loaded=loaded
  loadOrder=5
  }}
